schema:
  type: object
  properties:
    mapping:
      type: mapping
      description: MAPPING
      default:
        a: "A"
        b: "B"

expected: |
  # # MAPPING [mapping]
  # mapping:
  #   a: A
  #   b: B
